# We assume that there is already a bunch of stuff installed in the sagemaker container already:
# https://github.com/aws/deep-learning-containers/blob/master/pytorch/training/docker/2.0/py3/cu118/Dockerfile.gpu
# We target a framework_version='2.0'
# which should use cuda 11.8

accelerate==0.22.0
pytorch-lightning==1.9.5
torchmetrics==0.11.3
comet_ml
datasets
transformers==4.31.0
tensorboardX
peft==0.3.0
opencv-python-headless
lightning-bolts==0.7.0
